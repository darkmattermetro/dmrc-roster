name: dmrc_roster_app
description: DMRC Line 7 Roster Master PWA
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # Supabase
  supabase_flutter: ^2.3.0
  
  # Charts
  fl_chart: ^0.66.0
  
  # CSV Processing
  csv: ^5.1.1
  file_picker: ^6.1.1
  
  # UI
  google_fonts: ^6.1.0
  flutter_animate: ^4.3.0
  
  # Utilities
  intl: ^0.18.1
  shared_preferences: ^2.2.2
  url_launcher: ^6.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
